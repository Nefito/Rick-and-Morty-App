import { Button } from 'components'; 
import { styled } from 'theme';
  
export const CardButton = styled(Button)`
  display: flex;
  position: absolute;
  right: 20px;
  bottom: 0;
  margin-bottom: 10px;
`;
